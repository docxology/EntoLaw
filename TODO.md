# EntoLaw TODO

Forward-only backlog. Closed work is in `git log`, not here. Measured counts
live in their generators; re-run the gates before trusting any number below.

## Gate state

<!-- BEGIN GENERATED gate-state: scripts/render_gate_state.py -->

All 4 declared gates pass.

Generated by `uv run python scripts/render_gate_state.py`, which runs each
command below and quotes what that command printed. No result here is typed
by hand and none is carried forward: the whole block is rewritten from one
fresh run. The block carries no date, because a typed date can be advanced
without running anything; freshness is established by
`uv run python scripts/render_gate_state.py --check`, which re-runs every gate
and exits non-zero when the committed block disagrees.

```
uv run pytest tests/ -m "not live" --cov=src --cov-fail-under=90  # 134 passed; 1 skipped; Required test coverage of 90% reached. Total coverage: 98.88%
uv run ruff check src scripts tests                               # All checks passed!
uv run python scripts/check_module_map.py                         # module map: 0 violations
uv run python scripts/check_docs_inventory.py                     # inventory passed: config 9, docs 4, scripts 10, skills 0, source_modules 23, tests 17; markdown files checked: 53
```

<!-- END GENERATED gate-state -->

## Active publication-readiness work

- [ ] Prepare publication packaging across target platforms after proofreading: repository metadata, DOI field, release notes, and public/private boundary check.
